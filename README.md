# Status Checker for DreamCam + Stripchat (Chrome/Chromium Extension)

Расширение-“светофор” для быстрой проверки доступности ключевых доменов DreamCam и Stripchat.
Показывает, есть ли соединение до сервисов

- 🟢 **OK (HTTP 200)** — домен отвечает нормально
- 🟡 **Connected (HTTP не 200)** — соединение есть, но статус другой (редирект/доступ запрещён/не найден)
- 🔴 **Error / No connection** — домен недоступен (таймаут, DNS, блок, SSL и т.д.)

> Расширение **не обходит блокировки** и **не чинит WebRTC** — это диагностический инструмент.

---

## 0) Как скачать (Windows) — через Git

### Вариант A: Git clone (рекомендуется)
1) Установи Git (если ещё нет):  
   - Сайт: https://git-scm.com/download/win

2) Открой **PowerShell** или **CMD** и выполни:

```bash
cd %USERPROFILE%\Desktop
git clone https://github.com/TOFU-tj/Status-Checker-for-DreamCam.git

